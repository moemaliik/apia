# APIA needs exactly two third-party packages — both easy to justify in review:
requests>=2.31    # HTTP for the GitHub REST API and the LLM endpoint (no vendor SDK)
rich>=13.0        # the learning dashboard (dashboard.py) only; the agent core is stdlib
